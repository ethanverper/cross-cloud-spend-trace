"""Phase 3 analytics: unified spend model, anomaly detection, month-end
forecast, and an optimization-suggestion rules engine, all built as
portable PySpark code (works against a local SparkSession or a Databricks
notebook's ambient `spark` session — see `pipeline.py` and
`databricks_notebook.py`)."""
